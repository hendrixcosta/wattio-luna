{
  "name": "wattio-luna",
  "version": "1.0.0",
  "description": "Luna — agente de IA que enriquece chamados de suporte analisando o código-fonte com Claude Code sobre um repositório clonado localmente (somente leitura).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pino": "^9.3.2",
    "pino-http": "^10.2.0"
  }
}
